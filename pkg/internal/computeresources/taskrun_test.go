package computeresources_test

import "testing"

func TestInitContainersWithLimitRange(t *testing.T) {
	// Test with limitranges only-- no compute resources on steps, sidecars, overrides, etc
}

func TestStepContainersWithLimitRange(t *testing.T) {
	// Test with limitranges only-- no compute resources on steps, sidecars, overrides, etc
}

func TestSidecarContainersWithLimitRange(t *testing.T) {
	// Test with limitranges only-- no compute resources on steps, sidecars, overrides, etc
}

// test with just step resources

// test with just step overrides etc

// test with steps/step overrides and limitranges
